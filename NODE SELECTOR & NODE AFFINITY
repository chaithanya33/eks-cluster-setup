NODE SELECTOR = SHEDULE THE PODS ON PARTICULARY NODE 

NODE AFFINITY = WHERE IT MATCHES LABEL IT CAN SCHEDULE ON NODE OR ELSE IT CAN SCHEDULE ON OTHER NODE

NODE AFFINITY = 1) PREFFERED = LABEL
                2) REUIRED  = NODE SELECTOR
